<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" xmlns:xhtml="http://www.w3.org/1999/xhtml" width="100%" height="100%" viewBox="0 0 1920 1080">
  <foreignObject id="fo" width="1920" height="1080" x="0" y="0">
    <xhtml:html xmlns="http://www.w3.org/1999/xhtml" style="width:100%;height:100%;margin:0;padding:0;">
    <xhtml:head>
      <xhtml:style>
        body {
          margin: 0;
          padding: 0;
          width: 100%;
          height: 100%;
          display: flex;
          justify-content: center;
          align-items: center;
          background-color: #1a1a1a;
          font-family: sans-serif;
          color: white;
          transition: background-color 0.5s ease;
        }
        .card {
          background: rgba(255, 255, 255, 0.1);
          padding: 40px;
          border-radius: 15px;
          text-align: center;
          backdrop-filter: blur(10px);
          border: 1px solid rgba(255,255,255,0.2);
        }
        button {
          padding: 10px 20px;
          font-size: 16px;
          cursor: pointer;
          background: #007bff;
          color: white;
          border: none;
          border-radius: 5px;
          margin-top: 20px;
        }
        button:hover {
          background: #0056b3;
        }
      </xhtml:style>
    </xhtml:head>
    <xhtml:body>
      <div class="card">
        <h1>SVG HTML Proof of Concept</h1>
        <p>This HTML is running inside an SVG foreignObject.</p>
        <button onclick="changeColor()">Change Background Color</button>
      </div>

      <xhtml:script>
      //<![CDATA[
        function changeColor() {
          const colors = ['#1a1a1a', '#2d1b2e', '#1b2d2b', '#2d2b1b', '#1b1b2d'];
          const randomColor = colors[Math.floor(Math.random() * colors.length)];
          document.body.style.backgroundColor = randomColor;
          console.log("Color changed to: " + randomColor);
        }
        
        // Auto-resize logic for the SVG container
        window.addEventListener('resize', function() {
            var w = window.innerWidth;
            var h = window.innerHeight;
            var svg = document.querySelector('svg');
            var fo = document.querySelector('foreignObject');
            if(svg) {
                svg.setAttribute('width', w);
                svg.setAttribute('height', h);
                svg.setAttribute('viewBox', '0 0 ' + w + ' ' + h);
            }
            if(fo) {
                fo.setAttribute('width', w);
                fo.setAttribute('height', h);
            }
        });
      //]]>
      </xhtml:script>
    </xhtml:body>
    </xhtml:html>
  </foreignObject>
</svg>
